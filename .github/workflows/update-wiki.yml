name: Update Wiki

on:
  push:
    branches:
      - main

jobs:
  update-wiki:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '14'

      - name: Install Swagger CLI
        run: npm install -g swagger-cli

      - name: Generate API Documentation
        run: |
          swagger-cli bundle docs/openapi.yaml --outfile docs/api.html --type html

      - name: List files in docs directory
        run: ls -la docs

      - name: Clone Wiki Repository
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git clone https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.wiki.git wiki

      - name: List files in wiki directory before copying
        run: ls -la wiki

      - name: Copy Documentation to Wiki
        run: |
          cp docs/api.html wiki/API_Documentation.html
          cp docs/Explanations.md wiki/Home.md
          ls -la wiki  # List files to verify copying
          cd wiki
          git add .
          git commit -m "Update wiki documentation from main repository"
          git push

      - name: List files in wiki directory after push
        run: ls -la wiki
